version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - echo "Installing SAST dependencies..."
  build:
    commands:
      - echo "Running SAST scan..."
      - bash scripts/run-sast.sh
